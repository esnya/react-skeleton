'use strict';

import React, { PropTypes } from 'react';

var App = React.createClass({
    propTypes: {
    },

    render: function() {
        return (
                <div>React Skeleton</div>
               );
    },
});

module.exports = App;
