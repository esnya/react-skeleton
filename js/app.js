"use strict";

import React, { PropTypes } from "react";

var App = React.createClass({
    propTypes: {
    },

    render: function() {
        return (
                <div>React Skelton</div>
               );
    },
});

module.exports = App;
