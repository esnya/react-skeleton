'use strict';

import React, { PropTypes } from 'react';

let App = React.createClass({
    propTypes: {
    },

    render: function() {
        return (
                <div>React Skeleton</div>
               );
    },
});

module.exports = App;
